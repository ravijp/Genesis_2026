# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# Secrets — never commit
.env
*.key
*apikey*
*api_key*

# Run artifacts: regenerable output is ignored, but the pinned run and the
# response cache are COMMITTED so a judge can replay the demo with no keys.
artifacts/runs/*
!artifacts/runs/pinned/
data/generated/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
